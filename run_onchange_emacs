#!/usr/bin/env bash

REPO="$HOME/.emacs.d"
COMMIT="9fdaa4c3db4787f3e852c966fd2dc5184831c963"

if [ ! -d "$REPO" ]; then
  git clone https://github.com/hlissner/doom-emacs "$REPO"
fi

git -C "$REPO" fetch origin && \
  git -C "$REPO" checkout "$COMMIT"
